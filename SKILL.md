---
name: demand-triage-router
description: |
  当用户说“帮我录个需求”“记录一下这个BUG”“收一下这个反馈”，或直接发来系统需求、用户反馈、客户诉求、反馈聊天截图、文档、表格、工单、异常/Bug描述时使用。当用户说“初始化一下需求分拣skill”时，先初始化飞书多维表格。不用于完整PRD、排期或最终做/不做决策。
version: 0.3.3
---

# 需求分拣机

## 目标

把零散用户反馈变成产品经理的收件箱：日常先接住需求和 Bug，自动分拣到 `需求池 / Bug池`，减少即时打断；等产品经理有空时，再按状态和优先级集中处理。

需求池状态：

- `待观察`：需要继续跟进，信号还不够强。
- `待确认`：需要进一步和需求方沟通，补充信息。
- `待执行`：需求已经清楚，等待产品经理执行。
- `执行中`：产品经理正在处理，可能是原型设计或文档阶段。
- `开发中`：已经移交开发，正在开发。
- `已完成`：已经上线或完成交付。

核心原则：日常反馈默认自动收件，不逐条打断；AI 做提取、清洗、归类、聚类、缺失信息提示，并给出可解释的初步优先级；PM 保留最终取舍和产品判断权。

## 使用边界

使用本技能处理：

- 初始化飞书多维表格需求分拣系统。
- 把截图、文档、表格、聊天记录、工单、访谈纪要、社群消息整理入池，作为产品收件箱。
- 区分需求与 Bug，并为需求设置状态。
- 用户说“看一下待确认的需求”“列一下待观察需求”“看看执行中的需求”“开发中的需求有哪些”时，按状态查询需求池明细。
- 用户说“今天收了哪些”“处理一下待确认需求”“看一下待复现 Bug”等时，进入集中处理流程。

不要处理：

- 用户只想写完整 PRD、产品方案或项目排期。
- 用户要做泛行业研究、竞品分析或内容分析，而不是反馈分拣。
- 用户要求 AI 直接决定“做 / 不做”的最终产品取舍；此时改为输出证据和建议判断框架。

## 前置规则

- 涉及飞书多维表格时，必须使用 `lark-base`；首次配置、权限、身份和 scope 问题必须使用 `lark-shared`。
- 执行 `lark-cli base +...` 写操作前，按 `lark-base` 要求读取对应 reference 文档。
- 如果用户给的是 wiki 链接，先解析真实 `obj_token`，不要把 wiki token 直接当作 `--base-token`。
- 不要读取、输出或写入密钥、token、app secret、access token。
- 对用户上传材料保留“用户原话 / 截图线索 / 来源摘要”，不要只保留 AI 改写后的结论。
- Bug 的故障原因、修复原因或处理结论写入 `原因反馈` 字段，不要混写到 `环境信息`。
- 需求分拣 Base 的本地状态记录在 `data/base-state.json`。只记录 Base 链接、base token、表名和 table id，不记录任何密钥或 access token。

## 工作流

### 1. 判断用户意图

先判断用户是在做哪一类任务：

| 用户意图 | 处理方式 |
|---|---|
| 初始化需求分拣系统 | 读取 `references/lark-base-schema.md`，创建或补齐 Base、表、字段、视图 |
| 日常投喂单条反馈 | 读取 `references/triage-rules.md`，默认自动分析、分拣、写入飞书，再用一句话轻反馈 |
| 批量导入待处理反馈 | 读取 `references/triage-rules.md`，先拆分候选项；拆分或归类不确定时确认，确定时可批量写入 |
| 异常确认写入 | 仅当用户明确要求“先分析/先别写/确认后再写”，或操作会删除、覆盖、降级、改池且无法安全自动处理时，先轻量确认再写入 |
| 查询需求状态 | 按用户指定状态读取 `需求池` 明细，例如待观察、待确认、待执行、执行中、开发中、已完成 |
| 查询 Bug | 按状态、严重程度或关键词读取 `Bug池` 明细 |
| 集中处理收件箱 | 按优先级、状态、最近出现时间汇总需求和 Bug，并给出下一步建议 |

处理反馈或查询前先检查 Base 状态：

1. 如果用户本次提供了 Base 链接或 token，优先使用用户提供的信息，并在验证可访问后更新 `data/base-state.json`。
2. 如果用户没有提供 Base，先读取 `data/base-state.json`；如果记录存在且 Base 可访问，复用该 Base。
3. 如果没有状态文件、状态文件缺失关键字段、或记录的 Base 不可访问，就自动走初始化流程：新建一个飞书多维表格，并在同一个 Base 内创建两个池子。
4. 初始化成功后，必须把 Base 链接、base token、两张表的 table id 和更新时间写入 `data/base-state.json`，供下次使用。

### 2. 初始化飞书 Base

读取 `references/lark-base-schema.md` 后执行：

1. 判断是否已有可复用 Base：用户提供的 Base 优先，其次使用 `data/base-state.json`。
2. 如果没有可复用 Base，直接创建新 Base，默认名称为 `需求分拣机`。
3. 在同一个 Base 内新建或补齐两张表：`需求池`、`Bug池`。
4. 为每张表创建字段和常用视图。
5. 创建完成后，返回 Base 名称、可访问链接、表名、关键字段，以及后续如何投喂反馈。
6. 将 Base 信息保存到 `data/base-state.json`，供下次自动复用。

不要为了可选项打断用户；`folder-token`、时区、Base 名称没有特别要求时，用合理默认值。只有权限、登录、身份、scope 或用户明确要放入指定飞书目录时，才打断用户确认。

状态文件格式：

```json
{
  "base_name": "需求分拣机",
  "base_token": "base token",
  "base_url": "https://...",
  "tables": {
    "需求池": "table id",
    "Bug池": "table id"
  },
  "updated_at": "YYYY-MM-DDTHH:mm:ss+08:00"
}
```

如果状态文件存在但 Base 不可访问，先说明记录失效，再重新初始化并覆盖状态文件。

### 3. 接收和分析反馈

支持材料包括：

- 文本：用户直接粘贴的反馈、聊天记录、工单、访谈摘要。
- 截图：先提取可见文字和上下文，再标注截图来源。
- 文档 / 表格：按行或段落拆分为反馈候选项。
- 混合材料：先建立候选项列表，再逐条分析。

每条候选项至少提取：

- 用户原话或原始证据。
- 来源渠道。
- 用户类型或客户类型。
- 使用场景。
- 真实问题。
- 影响范围。
- 业务价值。
- 情绪强度或影响程度。
- Bug 的原因反馈；只有用户、研发或排查结果明确给出原因时填写，未知时留空。
- 是否重复或疑似同类问题。
- 缺失信息。
- 下一步动作。
- 拟入池、状态和优先级。

提取完成后，按模式处理：

- `日常收件模式`：默认模式。单条普通反馈自动写入飞书，写入后只返回一句轻反馈，减少打断。
- `异常确认模式`：仅在用户明确要求先确认，或操作会删除、覆盖、降级、改池且无法安全自动处理时使用。轻量确认只展示用户原话摘要、拟入池、状态和优先级；用户确认后再写入。

无论哪种模式，写入前内部必须已经完成 `用户类型 / 来源渠道 / 影响范围 / 业务价值 / 真实问题 / 使用场景 / 缺失信息 / 下一步动作` 的预处理，并一起写入。

命中以下任一条件时，进入异常确认模式：

- 用户明确说“先别写入”“先分析一下”“等我确认”“不要自动入池”。
- 批量材料拆成多条候选项，拆分边界、归类或优先级不确定。
- 写入会删除记录、覆盖重要字段、降低优先级或严重程度、改变已有记录池子，且无法通过新增记录或追加证据安全处理。
- 信息不足但必须二选一时，优先选择更保守的可追踪写法，不要为了确认而打断：疑似 Bug 进 `Bug池/待确认` 或 `待复现`；疑似需求进 `需求池/待确认`。
- `P0/P1` 需求或 `S0/S1` Bug 不触发写入前确认，应先入池，再在轻反馈里标出“需尽快处理”。

辅助字段判定原则：

- 能从上下文合理推断时就填写推断值，不要因为用户没有逐字说明就默认 `未知` 或 `其他`。
- `未知`、`其他`、`未定` 只是兜底值，不是默认值；只有缺少判断线索、信息互相矛盾或字段含义确实不适配时才使用。
- 优先级必须尽量评定为 `P0 / P1 / P2 / P3`，并能用影响范围、业务价值、用户类型或场景重要性解释；`未定` 只在完全无法判断或用户明确要求暂不评定时使用。

判断顺序：

1. 是否为明确故障、异常、不可用或与预期不符的问题；是则拟写入 `Bug池`。
2. 其余产品诉求、体验问题、机会线索、弱信号都拟写入 `需求池`，再用状态区分处理阶段。

需求状态判断：

- 场景、问题、影响都不清楚，但值得继续看：`待观察`。
- 场景或真实问题不够清楚，需要找需求方补信息：`待确认`。
- 真实问题清楚，已经可以由产品经理开始处理：`待执行`。
- 用户明确说正在做原型、方案或文档：`执行中`。
- 用户明确说已经移交开发或研发处理中：`开发中`。
- 用户明确说已上线、已修复、已交付：`已完成`。

需求优先级判断：

- `P0`：核心功能不可用、严重阻塞业务、数据丢失或高风险线上事故。
- `P1`：影响采购、续费、付费转化、核心客户、关键业务流程，或多个客户反复反馈。
- `P2`：场景清楚、问题明确，能改善效率、体验、激活或留存，但暂缺强收入或大范围证据。
- `P3`：弱信号、偏体验、个体偏好、影响较小，适合观察或低成本优化。
- `未定`：信息互相矛盾、完全无法判断，或用户明确要求暂不评定。

异常确认不需要固定话术。自然说明“我准备这样记录，你看是否要改”即可。

需求池确认只包含：

| 字段 | 说明 |
|---|---|
| 用户原话 | 两行以内摘要；过长时用 `...` 省略 |
| 拟入池 | `需求池` |
| 需求状态 | 待观察、待确认、待执行、执行中、开发中、已完成 |
| 优先级 | P0、P1、P2、P3、未定 |

Bug池确认可以包含：

| 字段 | 说明 |
|---|---|
| 用户原话 | 两行以内摘要；过长时用 `...` 省略 |
| 拟入池 | `Bug池` |
| Bug状态 | 待确认、待复现、待修复、修复中、待验证、已修复、无法复现、关闭 |
| 严重程度 | S0阻塞、S1严重、S2一般、S3轻微、未知 |

异常确认示例：

```text
我准备这样记录：

| 用户原话 | 拟入池 | 需求状态 | 优先级 |
|---|---|---|---|
| 客户希望批量导出项目数据，每周整理项目进度给老板... | 需求池 | 待执行 | P2 |

这样写可以吗？你也可以直接改状态或优先级。
```

异常确认模式下，用户明确回复“确认”“没问题”“可以入池”“按这个处理”等之后，才能写入飞书。用户要求修改时，先修正拟入池、状态或优先级，并再次确认。

### 4. 写入飞书

日常收件模式下，完成分析和预处理后直接写入飞书；异常确认模式下，必须等用户确认后写入。高优先级需求或严重 Bug 仍默认直接写入，只在反馈中提醒优先关注。

不要把“用户提出的方案”直接当成需求。先把方案还原成场景和真实问题。

写入前必须先完成 Base 检查：如果没有可用 Base，就先初始化并记录状态，然后再写入本条反馈。

写入前必须：

1. 读取目标表字段结构。
2. 判断字段可写性，只写存储字段。
3. 使用写入前已经完成的内部预处理结果写入辅助字段，不要在写入时临时把缺省字段统一填成 `未知`、`其他` 或 `未定`。
4. 对重复反馈优先更新已有记录的来源、最近出现时间、状态、优先级和下一步动作；不要无脑新增重复记录。
5. 更新 Bug 为已修复、关闭、无法复现或补充处理进度时，如用户提供原因、根因、处理备注或结论，写入 `原因反馈`；不要追加到 `环境信息`。
6. 批量写入时分批处理，避免超过 CLI 或 API 上限。

日常收件模式写入后只输出轻反馈：

```text
已收进需求池：待执行 / P2
标题：首页信息过多导致新建项目后下一步不清晰
```

批量或异常确认模式写入后向用户输出：

```text
本次写入完成：
- 需求池：新增 N 条 / 更新 N 条
- Bug池：新增 N 条 / 更新 N 条

需要你跟进：
1. ...
```

### 5. 查询需求或 Bug

当用户说“看一下待确认的需求”“待观察的需求有哪些”“列一下执行中的需求”“看看开发中的需求”“已完成需求汇总”等：

1. 先完成 Base 状态检查。
2. 读取 `需求池` 真实字段结构。
3. 按用户指定的 `状态` 筛选记录。
4. 输出明细。

需求查询输出：

```text
待确认需求共 N 条：

| 需求标题 | 用户原话摘要 | 使用场景 | 缺失信息 | 下一步动作 | 负责人 | 最近出现时间 |
|---|---|---|---|---|---|---|
```

当用户说“看一下待修复 Bug”“严重 Bug 有哪些”“开发中的 Bug”等：

1. 读取 `Bug池` 真实字段结构。
2. 按状态、严重程度或关键词筛选。
3. 输出 Bug 明细和建议下一步。

Bug 查询输出优先包含：

```text
| Bug标题 | 状态 | 严重程度 | 原因反馈 | 缺失信息/环境信息 | 负责人 | 最近出现时间 |
|---|---|---|---|---|---|---|
```

当用户说“今天收了哪些”“最近收了哪些反馈”“处理一下待确认需求”“帮我整理 P1/P2”“看一下待复现 Bug”等：

1. 先完成 Base 状态检查。
2. 分别读取 `需求池` 和 `Bug池` 字段结构。
3. 按状态、优先级、严重程度和最近出现时间组织结果。
4. 输出集中处理队列：先列高优先级和阻塞项，再列待确认/待复现项，最后列待观察项。
5. 对每条给出一句下一步建议，例如补充信息、联系需求方、安排复现、进入方案设计。

## 输出要求

日常收件模式输出：

1. **收件结果**：目标池子、状态、优先级或严重程度。
2. **记录标题**：一行标题。
3. **必要提醒**：只有命中高风险、缺权限或写入失败时才补充说明。

异常确认模式确认前输出：

1. **轻量确认**：用户原话摘要、拟入池、状态、优先级。
2. **待用户确认项**：需要用户确认拟入池、状态或优先级是否要改。

批量或异常确认后输出：

1. **处理结果**：新增、更新、跳过和失败数量。
2. **写入明细**：每条反馈的目标池子、状态、优先级。
3. **待跟进事项**：补证据、确认复现、PM 判断项。
4. **飞书写入结果**：Base 链接、表名、记录摘要；如未写入，说明缺少什么。

## 自检清单

- 是否保留用户原话或原始证据。
- 是否优先使用日常收件模式，避免对普通单条反馈逐条打断用户。
- 是否只在用户明确要求、批量拆分不确定，或会删除/覆盖/降级/改池且无法安全自动处理时进入异常确认模式。
- 是否在写入前已内部预处理用户类型、来源渠道、影响范围、业务价值、真实问题、使用场景、缺失信息和下一步动作。
- 是否在写入前检查 `data/base-state.json`；没有可用 Base 时是否先初始化两张表并记录链接。
- 是否区分 Bug 和需求。
- 是否用需求状态承载待观察、待确认、待执行、执行中、开发中、已完成。
- 是否给出可解释的初步优先级，并避免把 `未定` 当默认值。
- 是否标注缺失信息。
- 是否把 Bug 的原因、根因、修复说明写入 `原因反馈`，而不是塞进 `环境信息`。
- 是否读取真实 Base 字段后再写入或查询。
- 是否把日常收件结果说得足够短，把集中处理明细说得足够可执行。

## 参考文件

- `references/lark-base-schema.md`：两张表的字段、视图和初始化规则。
- `references/triage-rules.md`：反馈提取、Bug/需求判断和需求状态规则。
- `examples/sample-feedback-inputs.md`：典型输入和分拣示例。
- `data/base-state.example.json`：本地 Base 状态文件格式示例。
